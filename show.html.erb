<h1>My Rolies</h1>
<p><a href="/my_roly_cards/new">New Roly</a></p>

<div>Unassigned Business</div>
<table>
	<% @rolies.each do |roly| %>
	<tr>
		<td><a href="/my_roly_cards/view/<%= roly.id%>"><%= image_tag roly.photo.url(:thumb), :style => "width:50px;padding:10px;vertical-align:middle" %></a></td>
		<td><a href="/my_roly_cards/view/<%= roly.id%>"><%= roly.title %></a><br /><%= roly.address %><br /><img src="" width="16" height="16"><img src="" width="16" height="16"><img src="" width="16" height="16"></td>
		<td></td>
	</tr>
	<% end %>
</table>
<div>Fomento Geek</div>
<table>
	<% @rolies.each do |roly| %>
	<tr>
		<td><a href="/my_roly_cards/view/<%= roly.id%>"><%= image_tag roly.photo.url(:thumb), :style => "width:50px;padding:10px;vertical-align:middle" %></a></td>
		<td><a href="/my_roly_cards/view/<%= roly.id%>"><%= roly.title %></a><br /><%= roly.address %><br /><img src="" width="16" height="16"><img src="" width="16" height="16"><img src="" width="16" height="16"></td>
		<td></td>
	</tr>
	<% end %>
</table>

